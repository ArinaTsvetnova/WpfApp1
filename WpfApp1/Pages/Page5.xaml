<Page x:Class="WpfApp1.Pages.Page5"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="name" Text="{Binding film.Name}" FontSize="40" Foreground="#215756" HorizontalAlignment="Center" Grid.Row="0"/>
        <Button x:Name="BackToMain" Content="На главное меню" Width="110" Height="40" HorizontalAlignment="Right" Click="BackToMain_Click" Grid.Row="0"/>
            <StackPanel Grid.Row="1">
            <Image Source="{Binding film.Image}" Grid.Row="1" Height="200"/>
            <TextBlock x:Name="rating" Text="{Binding film.Rating}" FontSize="20" Foreground="#215756" HorizontalAlignment="Center"/>
            <TextBlock x:Name="ganre" FontSize="20" Foreground="#215756" HorizontalAlignment="Center"/>
            <TextBlock x:Name="opisanie" Text="{Binding film.Info}" FontSize="20" Foreground="#215756" HorizontalAlignment="Center"/>
        </StackPanel>
            <ListBox x:Name="FilmListBox"  Grid.Row="2" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="300" Height="70" Orientation="Horizontal">
                        <TextBlock Text="{Binding Date, StringFormat={}{0:d}}" Margin="5"/>
                        <TextBlock Text="{Binding Time}" Margin="5"/>
                        <TextBlock Text="{Binding Hall.ClassHall.Class}" Margin="5"/>
                            <Button x:Name="Buy" Content="Купить билет" Width="90" Height="40" Click="Buy_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <TextBlock x:Name="sessia" FontSize="20" Foreground="#215756" HorizontalAlignment="Center"/>
        
        
    </Grid>
</Page>
