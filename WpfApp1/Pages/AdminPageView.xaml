<Page x:Class="WpfApp1.Pages.AdminPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions> 
        <TextBlock Text="Admin" FontSize="30" Margin="10" Grid.Row="0"/>
        <ListBox x:Name="UserAll" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate >
                <DataTemplate>
                    <StackPanel Margin="15, 5" Orientation="Horizontal">
                        <StackPanel Margin="5" HorizontalAlignment="Center" Width="250">
                            <TextBlock TextWrapping="Wrap" Text="{Binding FullName}" FontSize="20"/>
                            <TextBlock TextWrapping="Wrap" Text="{Binding Phone}" FontSize="15"/>
                            <TextBlock TextWrapping="Wrap" Text="{Binding Role}" FontSize="15"/>
                            <TextBlock TextWrapping="Wrap" Text="{Binding IsFrozen}" FontSize="15"/>
                        </StackPanel>
                        <Button x:Name="Fruzen" Content="Заморозка" Width="95" Height="30" Click="Fruzen_Click" FontSize="15" Margin="5" Visibility="Visible"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="Back" Grid.Row="0" Content="На главную" Height="45" Width="150" FontSize="24" HorizontalAlignment="Right" Margin="10" Click="Back_Click"/>
    </Grid>
</Page>
