<Page x:Class="WpfApp1.Pages.AccountPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AccountPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <Button x:Name ="Exit" Content="На главную" Width="120" Height="40" Margin="5" HorizontalAlignment="Right" Click="Exit_Click"/>
            <TextBlock x:Name="TxtFullName" Text="ФИО Клиента" FontSize="30"/>
            <TextBlock x:Name="TxtPhone" Text="+7 (999) 000-00-00" FontSize="20" Margin="5"/>
            <TextBlock Text="История визитов и предстоящие процедуры" FontSize="15"  Margin="5"/>
        </StackPanel>

        <ListBox x:Name="HistoryGo" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="230" Height="450">
                        <TextBlock Text="{Binding AppointmentDateTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                        <TextBlock Text="{Binding ServiceTypes.Name}"/>
                        <TextBlock Text="{Binding Users.FullName}"/>
                        <TextBlock Text="{Binding FinalPrice, StringFormat={}{0} ₽}"/>
                        <TextBlock Text="{Binding Status}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <TextBlock Text="История покупок" FontSize="15"  Margin="5" Grid.Row="2"/>

        <ListBox x:Name="HistoryBuy" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="3" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="230" Height="450">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text="{Binding OrderDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                        <TextBlock Text="{Binding PickupDate, StringFormat={}{0:dd.MM}}"/>
                        <TextBlock Text="{Binding Status}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        
    </Grid>
</Page>
