<Page x:Class="WpfApp1.Pages.AccountPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AccountPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0">
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="TxtFullName" Text="ФИО Клиента" FontSize="30"/>
                <TextBlock x:Name="TxtPhone" Text="+7 (999) 000-00-00" FontSize="20" Margin="5"/>
            </StackPanel>
        </StackPanel>
        <TextBlock Text="История визитов и предстоящие процедуры" FontSize="15"  Margin="5"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Дата и время"/>
            <TextBlock Text="Услуга"/>
            <TextBlock Text="Мастер"/>
            <TextBlock Text="Цена"/>
            <TextBlock Text="Статус"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding AppointmentDateTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
            <TextBlock Text="{Binding ServiceType.Name}"/>
            <TextBlock Text="{Binding Master.FullName}"/>
            <TextBlock Text="{Binding FinalPrice, StringFormat={}{0} ₽}"/>
            <TextBlock Text="{Binding Status}"/>
        </StackPanel>

                                <DataGridTextColumn Header="№ Заказа" Binding="{Binding Id}" Width="0.5*"/>
                                <DataGridTextColumn Header="Дата заказа" Binding="{Binding OrderDate, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
                                <DataGridTextColumn Header="Дата получения" Binding="{Binding PickupDate, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
                                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat={}{0} ₽}" Width="*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*">             
    </Grid>
</Page>
