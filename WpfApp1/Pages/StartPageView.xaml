<Page x:Class="WpfApp1.Pages.StartPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="Shop" Content="Магазин косметики" Width="150" Height="35" Margin="5" Click="Shop_Click"/>
            <Button x:Name="LogIn" Content="Войти" Width="100" Height="35" Margin="5" Click="LogIn_Click"/>
            <Button x:Name="Account" Content="Мой аккаунт" Width="120" Height="35" Margin="5" Click="Account_Click"/>
        </StackPanel>
        <TextBlock Text="Добро пожаловать в «Космическую ложу»" FontSize="28" Margin="5" HorizontalAlignment="Center" Grid.Row="1"/>
        <StackPanel Grid.Row="2">
            <TextBlock Text="Запись на услугу" FontSize="20" Margin="5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Выберите услугу:" Margin="5"/>
                    <ComboBox x:Name="ListServiceType" Height="35" SelectionChanged="ListServiceType_SelectionChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Выберите мастера:" Margin="5"/>
                    <ComboBox x:Name="ListMasterType" SelectionChanged="ListMasterType_SelectionChanged" Height="35"/>
                </StackPanel>
            </Grid>
            <Button x:Name="BtnBook" Content="Записаться" Width="140" Height="35" Margin="10" Grid.Column="4" Click="BtnBook_Click"/>
        </StackPanel>
    </Grid>
</Page>
