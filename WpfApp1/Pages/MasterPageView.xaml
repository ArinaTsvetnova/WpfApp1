<Page x:Class="WpfApp1.Pages.MasterPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MasterPageView">

    <Grid Margin="0,0,22,-466">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Мастер" FontSize="30" Margin="10" Grid.Row="0"/>
        <Button x:Name="Back" Grid.Row="0" Content="На главную" Height="45" Width="150" FontSize="24" HorizontalAlignment="Right" Margin="10" Click="Back_Click"/>
        <TextBlock Text="Записи:" FontSize="25" Margin="5" Grid.Row="1"/>
        <ListBox x:Name="Zapis" Grid.Row="2" d:ItemsSource="{d:SampleData ItemCount=5}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate >
                <DataTemplate>
                    <StackPanel Margin="15, 5" Orientation="Horizontal">
                        <StackPanel Margin="5" HorizontalAlignment="Center" Width="250">
                            <TextBlock TextWrapping="Wrap" Text="{Binding ServiceTypes.Name}" FontSize="20"/>
                            <TextBlock TextWrapping="Wrap" Text="{Binding AppointmentDateTime}" FontSize="15"/>
                        </StackPanel>
                        <Button x:Name="Look" Content="Подробнее" Width="95" Height="30" FontSize="15" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <TextBlock Text="Услуги:" FontSize="25" Margin="5" Grid.Row="3"/>
        <ListBox x:Name="Yslug" Grid.Row="4" d:ItemsSource="{d:SampleData ItemCount=5}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate >
                <DataTemplate>
                    <StackPanel Margin="15, 5" Orientation="Horizontal">
                        <StackPanel Margin="5" HorizontalAlignment="Center" Width="250">
                            <TextBlock TextWrapping="Wrap" Text="{Binding ServiceTypes.Name}" FontSize="20"/>
                            <TextBlock TextWrapping="Wrap" Text="{Binding ServiceTypes.BasePrice}" FontSize="15"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="AddYslug" Content="Добавить услугу" Grid.Row="5" Margin="5" FontSize="20" Height="30" Width="200" Click="AddYslug_Click"/>
    </Grid>
</Page>
