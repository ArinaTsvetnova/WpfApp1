<Page x:Class="WpfApp1.Pages.AppointmentListPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentListPageView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Подтверждение записи" FontSize="20" HorizontalAlignment="Center" Margin="5"/>
        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="1">
                <TextBlock Text="Услуга:"/>
                <TextBlock x:Name="Service"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="1">
                <TextBlock Text="Мастер:"/>
                <TextBlock x:Name="Master"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="1">
                <TextBlock Text="Дата и время:"/>
                <TextBlock x:Name="Datatime"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="1">
                <TextBlock Text="Стоимость:"/>
                <TextBlock x:Name="Price"/>
            </StackPanel>

            <TextBlock Text="Способ оплаты:" FontWeight="SemiBold" Margin="5"/>
            <ComboBox x:Name="Pay"  Height="30"/>

            <!-- Комментарий --><!--
            <TextBlock Text="Комментарий (необязательно):" FontWeight="SemiBold" Margin="0,10,0,5"/>
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" 
                     Height="60" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        --><!-- Сообщения об ошибках --><!--
        <TextBlock Grid.Row="2" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   TextWrapping="Wrap"
                   Margin="0,10"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>

        --><!-- Кнопки управления --><!--
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Отмена" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            --><!-- Предполагаемый стиль --><!--

            <Button Content="Записаться" 
                    Width="120" 
                    Height="35"
                    Command="{Binding ConfirmCommand}"
                    IsEnabled="{Binding IsConfirmEnabled}"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            --><!-- Предполагаемый стиль -->
        </StackPanel>
    </Grid>
</Page>
