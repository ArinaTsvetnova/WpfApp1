<Page x:Class="WpfApp1.Pages.Page3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page3">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="UName" FontSize="40" Foreground="#215756" HorizontalAlignment="Center" Margin="5" Grid.Row="0"/>
        </StackPanel>
        <ListBox x:Name="UserInfoListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=6}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="250" Height="320">
                            <Image Source="{Binding Session.Film.Image}" />
                            <TextBlock Text="{Binding Film.Name}"/>
                            <TextBlock Text="{Binding Session.Film.Rating}"/>
                            <TextBlock Text="{Binding Session.SessionPlace.Place.Nomber}"/>
                            <TextBlock Text="{Binding Session.Date}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
    </Grid>
</Page>
