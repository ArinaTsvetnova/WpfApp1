<Page x:Class="WpfApp1.Pages.Page6"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page6">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Button x:Name="buy"  Height="40" Width="110" Content="Оформить билет" Margin="5"  HorizontalAlignment="Right" Grid.Row="0" Click="buy_Click"/>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="{Binding sessions.Date, StringFormat={}{0:d}}" FontSize="20" Foreground="#215756" Margin="5" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding sessions.Time}" FontSize="20" Foreground="#215756" Margin="5" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding sessions.Hall.ClassHall.Class}" FontSize="20" Foreground="#215756" Margin="5" HorizontalAlignment="Left"/>
            <ListBox x:Name="SitAddListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock x:Name="sits" Height="20" Width="20"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </StackPanel>
        <TextBlock x:Name="classh"  FontSize="20" Foreground="#215756" Grid.Row="0"/>
        <Rectangle Height="40" Width="600" Fill="HotPink" Grid.Row="1" Margin="5"/>
        <TextBlock FontSize="25" Text="ЭКРАН" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
        <ListBox Grid.Row="2" x:Name="SitListBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button x:Name="sit" Content="{Binding Place.Nomber}" Height="40" Width="40" Click="sit_Click"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="clear" Content="Сбросить" Height="40" Width="110" HorizontalAlignment="Right" Grid.Row="3" Click="clear_Click"/>
    </Grid>
</Page>
