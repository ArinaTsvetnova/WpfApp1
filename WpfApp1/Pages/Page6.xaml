<Page x:Class="WpfApp1.Pages.Page6"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page6">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding session.Date}" FontSize="20" Foreground="#215756" HorizontalAlignment="Left" Grid.Row="0"/>
        <TextBlock Text="{Binding session.Time}" FontSize="20" Foreground="#215756" HorizontalAlignment="Left" Grid.Row="0"/>
        <TextBlock x:Name="classh"  FontSize="20" Foreground="#215756" Grid.Row="0"/>
        <Rectangle Height="40" Width="600" Fill="HotPink" Grid.Row="1">
            <TextBlock FontSize="20" Height="40" Width="600" Text="ЭКРАН" />
        </Rectangle>
        <Button Height="40" Width="110" Content="Оформить билет" HorizontalAlignment="Right" Grid.Row="0"/>
        
    </Grid>
</Page>
